package daily;

public class Stringab {

    public int maximumGain(String s, int x, int y) {
        if (s.length() == 0) {
            return Math.max(x, y);
        }
        char ch = s.charAt(0);

    }
}